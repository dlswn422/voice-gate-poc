# ==================================================
# Core ML / LLM
# ==================================================

torch
# PyTorch
# - 모든 딥러닝 모델의 기반 프레임워크
# - LLM(Qwen), 임베딩 모델(MiniLM), Whisper 실행에 필수

transformers
# Hugging Face Transformers
# - Qwen, Whisper 등 LLM/STT 모델 로딩 및 추론
# - 현재 2차 대화 모델, 일부 실험용 모델에서 사용

accelerate
# Transformers 추론/로딩 최적화
# - 디바이스 매핑, 메모리 관리
# - GPU 없을 때도 안정적인 로딩을 위해 포함

pydantic
# 데이터 스키마 검증
# - IntentResult, DialogResult 등 구조 정의
# - AppEngine ↔ NLU ↔ DB 사이 인터페이스 안정화


# ==================================================
# Audio / Speech-to-Text (STT)
# ==================================================

sounddevice
# 마이크 입력 수집
# - 실시간 음성 입력 (키오스크, PC 마이크)

numpy>=1.23.0
# 수치 연산 핵심 라이브러리
# - 오디오 파형 처리
# - 임베딩 벡터 계산
# - STT / Intent Embedding 공통 의존성

scipy
# 신호 처리 라이브러리
# - 오디오 리샘플링 (48kHz → 16kHz 등)

faster-whisper
# Whisper 기반 고속 STT 엔진
# - 실시간 음성 인식
# - CPU 환경에서도 안정적으로 동작

openai-whisper
# Whisper 원본 패키지
# - 실험/비교용
# - 일부 환경에서 faster-whisper fallback 용도


# ==================================================
# Intent Classification (Embedding 기반 1차 분류)
# ==================================================

sentence-transformers>=2.2.2
# 문장 임베딩 모델 래퍼
# - all-MiniLM-L6-v2 사용
# - LLM 없이 CPU에서 빠른 의도 분류 (50~100ms)

scikit-learn>=1.2.0
# 머신러닝 유틸
# - cosine_similarity 사용
# - intent_embedding.py에서 임베딩 간 유사도 계산


# ==================================================
# Network / External API
# ==================================================

requests
# HTTP 클라이언트
# - Ollama API 호출 (Intent-1 LLM, Dialog LLM)
# - 외부 서비스 연동


# ==================================================
# Database / Environment
# ==================================================

psycopg2-binary
# PostgreSQL 드라이버
# - Supabase(Postgres) 연동
# - intent_logger / dialog_logger에서 사용

python-dotenv
# 환경변수 관리
# - .env 파일 로드
# - DATABASE_URL, OLLAMA_BASE_URL 등 관리